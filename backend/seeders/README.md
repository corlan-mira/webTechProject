/**
 * Seeders Folder
 * 
 * Database seed files for Sequelize
 * Populate development database with test data
 * 
 * Naming convention: [timestamp]-[name].js
 * Example: 20251202120100-demo-users.js
 * 
 * Structure:
 * module.exports = {
 *   up: async (queryInterface, Sequelize) => {
 *     // Insert data
 *   },
 *   down: async (queryInterface, Sequelize) => {
 *     // Remove data
 *   }
 * };
 * 
 * Commands:
 * npm run seed:all      - Run all seeders
 * npm run seed:undo     - Undo last seeder
 * npm run seed:undo:all - Undo all seeders
 * 
 * Include:
 * - Demo users (organizers)
 * - Demo event groups
 * - Demo events
 * - Demo attendance records
 */
